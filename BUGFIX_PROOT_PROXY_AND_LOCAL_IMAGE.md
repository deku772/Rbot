# Bug 修复：PRoot 流量代理测试失败 & 本地镜像未能检测到

## 问题描述

1. **PRoot 流量代理测试失败**
   - 代理测试使用单一测试 URL (`registry.npmmirror.com`)
   - 该 URL 可能不稳定或被墙，导致所有代理测试失败
   - 超时时间过长（8s 连接 + 15s 读取），影响用户体验

2. **本地镜像未能检测到**
   - 即使本地存在完整的 rootfs 镜像文件，也会被删除并重新下载
   - MD5 校验失败时没有详细日志，难以排查问题
   - PRoot 模式下 MD5 计算可能因权限问题失败
   - 没有容错机制，文件大小合理时也会因 MD5 失败而删除

## 修复方案

### 1. 改进代理测试逻辑 (`GitHubProxyManager.java`)

#### 修改 `testProxies()` 方法
- **多 URL 测试**：使用多个测试 URL，提高成功率
  - `registry.npmmirror.com` (npm 镜像)
  - `mirrors.tuna.tsinghua.edu.cn` (清华镜像)
  - `api.github.com/zen` (GitHub API)
- **容错机制**：任一 URL 成功即可，全部失败则使用直连

#### 优化 `testWithCurl()` 方法
- **减少超时时间**：
  - 连接超时：8s → 5s
  - 读取超时：15s → 10s
- **使用 HEAD 请求**：更快，不需要下载内容
- **改进错误处理**：
  - 区分超时、DNS 失败等不同错误类型
  - 添加 finally 块确保连接关闭
- **添加详细日志**：便于排查网络问题

### 2. 改进本地镜像检测 (`SetupActivity.java`)

#### 修改 `findRootfsTarball()` 方法
- **文件大小检查**：
  - 显示文件大小信息
  - 基本大小检查（至少 400MB）
- **智能容错**：
  - MD5 校验失败时，在 PRoot 模式下检查文件大小
  - 如果文件大小合理（450-500MB），尝试使用本地镜像
  - 给用户提示，说明可以跳过校验
- **详细日志**：
  - 显示期望和实际的 MD5 值
  - 说明删除原因

#### 改进 `verifyRootfsMd5()` 方法
- **详细进度提示**：
  - 开始校验时提示
  - 显示期望和实际 MD5 值
  - 说明使用的校验方法（Java 或 md5sum）
- **增加超时时间**：md5sum 命令超时从 30s 增加到 60s
- **更好的错误处理**：区分不同的失败原因

### 3. 增强 MD5 计算 (`PRootManager.java`)

#### 改进 `computeMd5()` 方法
- **文件检查**：
  - 检查文件是否存在
  - 检查文件是否可读
- **详细日志**：
  - 记录文件路径和大小
  - 记录计算耗时
  - 记录最终 MD5 结果
- **细化错误处理**：
  - 区分 FileNotFoundException、IOException 等
  - 记录具体错误类型和消息

## 测试建议

### 测试场景 1：代理测试
1. 在不同网络环境下测试（WiFi、移动网络、VPN）
2. 验证多 URL 容错机制是否生效
3. 检查日志中的代理测试结果

### 测试场景 2：本地镜像检测
1. **正常情况**：放置正确的 rootfs 文件，验证 MD5 通过
2. **文件损坏**：放置不完整的文件（< 400MB），验证被删除
3. **MD5 失败但文件完整**：
   - 放置大小正确但 MD5 不匹配的文件
   - 在 PRoot 模式下验证是否使用本地文件
   - 在 Chroot 模式下验证是否重新下载

### 测试场景 3：MD5 计算
1. 验证大文件（458MB）MD5 计算是否正常
2. 检查日志中的 MD5 计算详情
3. 测试文件权限问题的处理

## 预期效果

1. **代理测试更稳定**：
   - 成功率提高（多 URL 容错）
   - 速度更快（减少超时、使用 HEAD 请求）
   - 更好的用户体验

2. **本地镜像检测更智能**：
   - 减少不必要的重新下载
   - PRoot 模式下更宽容的检测策略
   - 更清晰的错误提示

3. **更好的可调试性**：
   - 详细的日志输出
   - 清晰的错误信息
   - 便于排查问题

## 相关文件

- `app/src/main/java/app/rbot/GitHubProxyManager.java`
- `app/src/main/java/app/rbot/SetupActivity.java`
- `app/src/main/java/app/rbot/PRootManager.java`

## 注意事项

1. 这些修改向后兼容，不会影响现有功能
2. 增加的日志有助于用户和开发者排查问题
3. PRoot 模式的容错机制可能允许使用 MD5 不匹配的文件，需要在实际使用中验证
