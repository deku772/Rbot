pluginManagement {
    repositories {
        google()
        mavenLocal()
        mavenCentral()
    }
    plugins {
        id("com.android.application") version "8.13.2"
        id("com.android.library") version "8.13.2"
        id("org.jetbrains.kotlin.android") version "2.1.21"
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}

// BotDrop chroot branch: only the app module
// Termux modules (terminal-emulator, terminal-view, termux-shared, api/*, server, starter, shell, manager)
// are no longer needed — all Linux operations run via su + chroot.
include ':app'
