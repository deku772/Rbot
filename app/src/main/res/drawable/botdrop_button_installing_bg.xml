<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Installing/disabled state: blue to indicate progress -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/botdrop_accent_blue"/>
            <corners android:radius="8dp"/>
        </shape>
    </item>
    <!-- Enabled state: gold accent -->
    <item>
        <ripple android:color="@color/botdrop_accent_dark">
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/botdrop_accent"/>
                    <corners android:radius="8dp"/>
                </shape>
            </item>
        </ripple>
    </item>
</selector>
